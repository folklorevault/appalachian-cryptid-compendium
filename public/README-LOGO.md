# Logo File Needed

The `appalachian-cryptid-logo.svg` was **8.6MB** because it contained an embedded PNG image, not actual vector paths.

## To Fix:

Create a new optimized logo:

1. **Size**: 200x200px (or 400x400px for retina)
2. **Format**: PNG or WebP
3. **Target size**: 10-20KB max
4. **Compression**: Use TinyPNG, Squoosh, or ImageOptim

### If you want a TRUE vector SVG:
- Re-export from your design tool
- Make sure "Embed Images" is UNCHECKED
- The file should be < 50KB if it's real vector

### Filename:
Save as: `appalachian-cryptid-logo.png` or `appalachian-cryptid-logo.svg`

The code will need to be updated to reference the new filename.
